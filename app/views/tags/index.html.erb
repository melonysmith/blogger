<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag, tag_path(tag) %>
    <%= link_to 'delete', tag_path(tag), method: :delete, 
  data: {confirm: 'Are you sure?'} %></li>
  <% end %>
</ul>
